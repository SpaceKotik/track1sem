#include "consts.h"
/*int testSolveEquation (int testsCount, int (*testFunc)(int testNum)) {
	int i;
	int isOK = 1;
	for (i = 0; i < testsCount; ++i) {
		if(0 != testFunc(i))
			isOK = 0;
	}
	if (isOK)
		return OK;
	else
		return ERROR;
}
*/